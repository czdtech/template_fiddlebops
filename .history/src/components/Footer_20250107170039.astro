---
const currentYear = new Date().getFullYear();

const socialLinks = [
  {
    url: "https://github.com/your-github",
    icon: "fa-github",
    label: "GitHub",
  },
  {
    url: "https://twitter.com/your-twitter",
    icon: "fa-twitter",
    label: "Twitter",
  },
  {
    url: "https://discord.gg/your-discord",
    icon: "fa-discord",
    label: "Discord",
  },
];

const quickLinks = [
  { href: "#play-fiddlebops", text: "开始游戏" },
  { href: "#features", text: "游戏特色" },
  { href: "#faq", text: "常见问题" },
];
---

<footer class="bg-card/80 backdrop-blur-md py-12 mt-24">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8 md:gap-12">
      <section>
        <h2 class="text-2xl font-bold text-accent mb-6">关于 FiddleBops</h2>
        <p class="text-text">
          一款由粉丝创作的音乐创作游戏，让每个人都能轻松创作音乐。
        </p>
      </section>

      <nav>
        <h2 class="text-xl font-bold text-accent mb-4">快速链接</h2>
        <ul class="space-y-2">
          {
            quickLinks.map(({ href, text }) => (
              <li>
                <a
                  href={href}
                  class="text-text hover:text-secondary transition-colors duration-200"
                >
                  {text}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>

      <section>
        <h2 class="text-xl font-bold text-accent mb-4">关注我们</h2>
        <div class="flex space-x-4">
          {
            socialLinks.map(({ url, icon, label }) => (
              <a
                href={url}
                class="text-text hover:text-secondary transition-colors duration-200"
                aria-label={label}
                rel="noopener noreferrer"
                target="_blank"
              >
                <i class={`fab ${icon} text-2xl`} aria-hidden="true" />
                <span class="sr-only">{label}</span>
              </a>
            ))
          }
        </div>
      </section>
    </div>

    <div class="text-center mt-8 pt-8 border-t border-background-light">
      <p class="text-text">
        © {currentYear} FiddleBops. 保留所有权利。
        <span class="text-secondary">由粉丝用❤️制作</span>
      </p>
    </div>
  </div>
</footer> 